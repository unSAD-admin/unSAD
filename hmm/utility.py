"""
this file contains data class needed by our model.

a model class, which contains all the global parameters
for the model. after saving it, we should be able to 
retrieve it from the saved model.

a datasets  container class which contains the global
parameter for the datasets such as sequence length L.
It also contains all the sequences.

a state sequence container class which is a basic sequence of
length L.

"""
